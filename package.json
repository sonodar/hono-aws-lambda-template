{
  "name": "hono-aws-lambda-template",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local/index.ts",
    "lint": "eslint .",
    "format": "eslint --fix .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sam:build": "sam build",
    "sam:validate": "sam validate --lint"
  },
  "dependencies": {
    "hono": "^4.4.5"
  },
  "devDependencies": {
    "@hono/eslint-config": "^0.0.6",
    "@hono/node-server": "^1.11.2",
    "@types/node": "^20.14.2",
    "esbuild": "^0.21.5",
    "tsx": "^4.15.2"
  }
}
